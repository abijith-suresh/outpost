name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.job }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22
          - 24

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Setup npm
        run: npm install --global npm@11.16.0

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        if: matrix.node-version == 22
        run: npm run format:check

      - name: Lint
        if: matrix.node-version == 22
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck --workspace @abijith-suresh/outpost

      - name: Test
        run: npm run test --workspace @abijith-suresh/outpost

      - name: Build
        run: npm run build --workspace @abijith-suresh/outpost

      - name: Installed-package smoke test
        run: npm run test:smoke --workspace @abijith-suresh/outpost

      - name: Verify website
        if: matrix.node-version == 22
        run: npm run verify --workspace outpost-website

      - name: Build website
        if: matrix.node-version == 22
        run: npm run build --workspace outpost-website
